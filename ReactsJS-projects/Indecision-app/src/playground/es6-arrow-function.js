function square(x) {
    return x * x;
}

console.log (square(3));

const squareArrow = (x) => {
    return x * x;
}

// comments can be added
// regular arrow function and arrow function using shorthand-syntax
console.log(squareArrow(9));